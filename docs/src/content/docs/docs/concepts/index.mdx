---
title: Concepts
description: The mental model behind keelson, one organizing idea per page.
sidebar:
  order: 0
---

import { LinkCard } from "@astrojs/starlight/components";

The concept pages build the mental model: what keelson is, why it is shaped
this way, and the vocabulary you will meet everywhere else. Each page carries
one organizing idea, and they read best in order: the map first, then the live
chat loop, the extension model, the two engines, the state behind them, and the
provider underneath every turn.

<LinkCard
  title="Architecture"
  href="architecture/"
  description="The map: what the harness owns, where ribs attach, and the one rule that keeps them apart."
/>
<LinkCard
  title="Chat"
  href="chat/"
  description="The live loop: one agent turn, the tools ribs give it, the provider you choose, and the governance that gates every call."
/>
<LinkCard
  title="The rib model"
  href="the-rib-model/"
  description="Why capabilities are packages, what a rib can contribute, and the boundary the harness enforces rather than requests."
/>
<LinkCard
  title="Workflows"
  href="workflows/"
  description="Determinism next to an agent: the DAG execution model, the two data channels, and where the agency is bounded."
/>
<LinkCard
  title="Snapshots and surfaces"
  href="snapshots-and-surfaces/"
  description="Ribs publish data, never UI: frames, the triple fail-closed pipeline, and the closed canvas vocabulary."
/>
<LinkCard
  title="Memory and state"
  href="memory-and-state/"
  description="One home, one database, one keychain, and how a memory earns the right to instruct the agent."
/>
<LinkCard
  title="Providers"
  href="providers/"
  description="The swappable agent: one interface, several coding-agent SDKs behind it, chosen per turn and per node."
/>
<LinkCard
  title="Glossary"
  href="glossary/"
  description="The vocabulary in one place, with the two overloaded terms disambiguated."
/>
