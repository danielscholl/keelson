---
title: Starter workflows
description: The bundled workflow catalog, grouped by the job each does, with a node-by-node walkthrough for each.
sidebar:
  order: 0
---

import { Aside } from "@astrojs/starlight/components";
import WorkflowCards from "../../../../components/WorkflowCards.astro";

Keelson ships eleven workflows. They are real, runnable, and the place the
[authoring recipes](../guides/authoring-workflows/) show up composed: a
three-line approval-gate recipe is one thing, the same gate sitting inside a
twenty-five-node investigate-and-implement pipeline is where the shape clicks. This
catalog groups what ships by the job it does, and each one gets a full
node-by-node walkthrough.

Each is also readable in a single screen, so opening the file is its own
documentation.

## Where these come from

The eleven are **bundled** starters: they ship with the package and seed into
your home (`<home>/workflows/`) on first run, so `keelson workflow list` shows
them as `global` once seeded. Copy and edit one freely; the bundled original
stays as a clean fallback. See [scopes](../reference/workflow-nodes/#scopes) for
how the layers merge.

Run any of them the same way, streaming the DAG as it executes:

```sh
keelson workflow run smoke-test --watch
keelson workflow run fix-issue --inputs ARGUMENTS="42"
```

<Aside type="note" title="What the Copilot workflows need">
Nine of the eleven set `provider: copilot` and `model: auto`, so they need a
GitHub Copilot subscription registered. The GitHub-facing ones (`fix-issue`,
`pr-review`, `plan-act-evaluate`, `resolve-comments`, `converge-pr`) also drive
the `gh` CLI, so it must be installed and authenticated. The approval and
interactive workflows pause for a human, which needs a running server (`keelson
start`). The other two pin no provider: `smoke-test` runs provider-free on the
stub with no credentials, and `design-artifact` leaves the provider open so
authoring tracks the strongest model the server offers.
</Aside>

## Ship code

The delivery flagships: an issue taken to a draft PR, a scoped PR review, a
plan-first lifecycle, a PR review thread resolver, and a PR convergence loop.
Each composes gates, loops, and branches into a real pipeline, and each has a
full walkthrough.

<WorkflowCards names={["fix-issue", "pr-review", "plan-act-evaluate", "resolve-comments", "converge-pr"]} />

## Plan and shape

Exploration workflows: turn a fuzzy idea into a validated requirements doc,
pressure-test a build by pitting a generator against an evaluator, or stress-test
any artifact — a claim, design, or plan — to a consensus verdict.

<WorkflowCards names={["interactive-prd", "adversarial-build", "adversarial-review"]} />

## Author workflows

<WorkflowCards names={["workflow-builder"]} />

## Produce artifacts

Author and publish a designed, self-contained HTML page to the canvas, a report
or briefing or chart, validated fail-closed on the way out.

<WorkflowCards names={["design-artifact"]} />

## Verify the engine

<WorkflowCards names={["smoke-test"]} />

## Related

- [Authoring workflows](../guides/authoring-workflows/): the recipes these
  compose, and the loop you follow to write one.
- [Workflow nodes](../reference/workflow-nodes/): the full schema every node
  here is built from.
- [Workflows](../concepts/workflows/): why deterministic YAML sits next to an
  agent, and the run lifecycle a workflow leaves behind.
